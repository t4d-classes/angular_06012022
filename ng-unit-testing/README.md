# Angular Unit Testing Demos

To run the unit tests, execute the following command from within the project folder.

```bash
npm test
```

A Chrome web browser window will open and execute the tests using the Karma test runner.
